{
  "name": "ippo",
  "private": true,
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "pnpm -r --parallel --filter @ippo/api --filter @ippo/web dev",
    "dev:web": "pnpm --filter @ippo/web dev",
    "dev:api": "pnpm --filter @ippo/api dev",
    "build": "pnpm -r build",
    "db:generate": "pnpm --filter @ippo/api db:generate",
    "db:migrate:local": "pnpm --filter @ippo/api db:migrate:local",
    "db:seed:local": "pnpm --filter @ippo/api db:seed:local",
    "setup:local": "pnpm --filter @ippo/api setup:local",
    "typecheck": "pnpm -r typecheck"
  },
  "volta": {
    "node": "22.23.1"
  }
}
